const initialState =
{
  id: null,
  name: '',
  image: ''
}

 const singleBelongingReducer = (state=initialState, action) => {

   switch (action.type) {

     default:
      return state;

   }
}


 export default singleBelongingReducer
